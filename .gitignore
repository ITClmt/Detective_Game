# deps (root + per-workspace)
node_modules/

# env
.env
.env.*.local

# logs
*.log

# OS
.DS_Store

# contenu narratif des enquêtes — pas publié dans le repo public,
# reste en local comme fixture pour les tests de packages/shared
content/
